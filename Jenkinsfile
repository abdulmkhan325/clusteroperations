pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout code from Git repository
                sh """
                    pwd
                    ls
                    env
                """.stripIndent()
            }
        }
        // Add more stages as needed for your pipeline sdfgsgfds
    }
}
